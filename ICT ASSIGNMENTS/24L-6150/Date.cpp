//#include "Date.h"
//
//Date::Date(int d, int m, int y) : day(d), month(m), year(y) {}
//
//int Date::operator-(const Date& other) const {
//    return (day - other.day) + (month - other.month) * 30 + (year - other.year) * 365;
//}
